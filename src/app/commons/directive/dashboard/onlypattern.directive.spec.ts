import { OnlypatternDirective } from './onlypattern.directive';

describe('OnlypatternDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlypatternDirective();
    expect(directive).toBeTruthy();
  });
});
